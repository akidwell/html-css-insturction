import {Product} from "./product";

// create an instance of product
let p1: Product = new Product("java", "Murach's java", 57.50);

// print the information of product to console
console.log(p1.about());
